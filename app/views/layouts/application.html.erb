<!DOCTYPE html>
<html>
  <head>
    <title>Whereweatelast</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <!-- This nav bar comes straight from Bootstrap-->
    <nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <%= link_to 'Where We Ate Last', root_path, class: 'navbar-brand' %>

      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">

          <% if user_signed_in? %>

            <span class="navbar-text w-100 text-right">
              Logged in as <%= current_user.email %> <%= link_to '(Sign out)', destroy_user_session_path, method: :delete %>
            </span>            

          <% else %>

            <!-- This is a dirty hack, but it aligns the other 2 nav items properly -->
            <ul class="navbar-nav mr-auto mt-2 mt-md-0">
            </ul>

            <ul class="navbar-nav">

              <li class="nav-item active">
                <%= link_to 'Log in', new_user_session_path, class: 'nav-link' %>
              </li>
              <li class="nav-item active">
                <%= link_to 'Sign up', new_user_registration_path, class: 'nav-link' %>
              </li>
              
            </ul>

          <% end %>

        </ul>

      </div>
    </nav>

    <!-- These flash alerts are required, as part of Devise's user functionality -->
    <% if notice.present? %>
      <p class="alert alert-info"><%= notice %></p>
    <% end %>
    <% if alert.present? %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
